# ⚠️ DO NOT run `terraform apply` directly for CF worker changes

Use `scripts/deploy-cf.sh` instead. Raw terraform apply will wipe worker secrets.

See: https://github.com/ColeMurray/background-agents/issues/XXX
